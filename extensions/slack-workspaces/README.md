# Slack Workspaces

Search and open locally available Slack workspaces from the Slack desktop app.

This extension reads the local Slack desktop state to discover workspaces, including:

- workspace names
- workspace domains and URLs
- workspace icons
- current selected workspace
- unread and highlight indicators
- workspace theme colors

It does not require a Slack API token.
